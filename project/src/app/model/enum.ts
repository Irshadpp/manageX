export enum Role {
    OWNER = "owner",
    MANAGER = "manager",
    EMPLOYEE = "employee"
}

export enum ProjectStatus{
    PLANNING = "planning",
    ACTIVE = "active",
    COMPLETED = "completed",
    BACKLOG = "backlog",
}

export enum Priority{
    LOW = "low",
    MEDIUM = "medium",
    HIGH = "high", 
}

export enum DurationType{
    MINUTES = "minutes",
    HOURS = "hours",
    DAY = "day"
}